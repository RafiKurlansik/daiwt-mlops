mlflow_params:
  run_name: 'deeping_forest'

pipeline_params:
  test_size: 0.25
  random_state: 42

model_params:
  n_estimators: 100
  max_depth: 3
  min_samples_leaf: 1
  max_features: 'auto'
  random_state: 42