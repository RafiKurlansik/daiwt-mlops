mlflow_params:
  run_name: 'new_forest'

pipeline_params:
  test_size: 0.2
  random_state: 206

model_params:
  n_estimators: 150
  max_depth: 44
  min_samples_leaf: 1
  max_features: 'auto'
  random_state: 206